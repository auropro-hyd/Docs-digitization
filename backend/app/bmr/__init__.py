"""BMR audit subsystem.

Implements the specs under ``/specs/001-bmr-audit-pipeline`` through
``/specs/005-rule-spec-and-authoring``. Lives alongside the existing
``app/compliance`` and ``app/workflow`` subpackages without replacing them
(Constitution VII — existing framework is the backbone).
"""
